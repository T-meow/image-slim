{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main image-slim window to use core APIs and local file dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}

